<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Vote-Live</title>
</head>
<body class="font-sans">
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <div class="text-center">
            </div>
            <a class='text-black-700' href="/">Go Home</a>
            <h6 class="py-6 text-center">Sign Up as a new Admin</h6>

            <div>
                <% if(messages && messages.error && messages.error.length > 0)  { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                            <li class="rounded-full border-2 p-2 bg-red-700 text-white"> <%= messages.error[i] %></li>
                    <% } %>
                  <% } %>
            </div> 
            
            <form action="/admin.ejs" method="post">
                <div class="py-3">
                    <label for="firstName">First Name:</label>
                    <input type="text" name="firstName" 
                    class="border border-gray-700 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <label for="lastName">Last Name:</label>
                    <input type="text" name="lastName"
                    class="border border-gray-700 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <label for="email">Email:</label>
                    <input type="email" name="email"
                    class="border border-gray-700 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <label for="password">password:</label>
                    <input type="password" name="password"
                    class="border border-gray-700 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Sign Up</button>
                </div>
            </form>
        </div>
         
    </div>
</html>